<header>
    <a href="#">
        <h1 class="site-title"><i class="fas fa-image"></i>IKKEditor</h1>
    </a>

    <nav class="navbar">
        <ul class="navlist">
            {{#each navlinks}}
            <li class="navitem navlink {{#if active}}active{{/if}}">
            <a href="/{{str}}">{{str}}</a>
            </li>
            {{/each}}
        </ul>
    </nav>
</header>